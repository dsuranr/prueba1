git --version
git config --global user.name "daniel"
git config --global user.email "d.uran1@pascualbravo.edu.co"

mkdir carpeta
cd carpeta

git init
git status

git add readme.txt
git commit -m "carpeta creada"

git config --global user.username dsuranr